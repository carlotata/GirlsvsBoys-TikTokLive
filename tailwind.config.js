/** @type {import('tailwindcss').Config} */
module.exports = {
   content: ["./*.html", "./src/**/*.{html,js}"],
   theme: {
      extend: {
         // paste the theme.extend block from the duck-race file here
         // (fontFamily, colors, keyframes, animation)
      },
   },
   plugins: [],
};
